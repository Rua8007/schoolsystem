<%= form_for(@feebreakdown) do |f| %>
  <% if @feebreakdown.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feebreakdown.errors.count, "error") %> prohibited this feebreakdown from being saved:</h2>

      <ul>
      <% @feebreakdown.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :grade_id %><br>
    <%= f.collection_select :grade_id , Batch.last.grades.order(:name), :id , :full_name,{},{:style => 'height:34px;width: 30%',  class: 'form-control'} %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title,class: 'form-control',:style => 'height:34px;width: 30%' %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount,class: 'form-control',:style => 'height:34px;width: 30%' %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>
