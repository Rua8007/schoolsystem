<% if current_user.role.rights.where(value: "create_fee").any? %>
	<div class="bg-light lter b-b wrapper-md">
	  <h1 class="m-n font-thin h3"> Submit Fee </h1>
	</div>
	<div style="margin-top: 2%;">
		<%= render 'form' %>
	</div>
<% end %>