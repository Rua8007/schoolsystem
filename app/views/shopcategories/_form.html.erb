<%= form_for(@shopcategory) do |f| %>
  <% if @shopcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shopcategory.errors.count, "error") %> prohibited this shopcategory from being saved:</h2>

      <ul>
      <% @shopcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
